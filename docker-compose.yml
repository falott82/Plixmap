services:
  deskly:
    build: .
    ports:
      - '8787:8787'
    volumes:
      - deskly_data:/app/data
    environment:
      - DESKLY_TRUST_PROXY=1
    restart: unless-stopped

volumes:
  deskly_data:
